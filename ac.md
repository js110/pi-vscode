# pi-vscode 验收标准（AC）

| 项 | 值 |
|----|-----|
| 来源文档 | PRD v1.4（prd/PRD.md，2026-09-14）；以 PRD 第 14 节基线 AC 扩展（v1.0 首批 23 条 + Phase 6 评审补充 9 条 + Phase 8 评审补齐 35 条） |
| 生成日期 | 2026-09-14 |
| 文档定位 | 验收兜底文档：全部条目为"系统应该怎样"的断言。技术方案必须能解释如何满足每条 AC；测试用例从 AC 衍生 |
| 编号体系 | G-{N} 业务目标；AC-RULE-{NN} 全局规则（不计入 e2e 分母）；AC-FN-{NN} 功能验收；AC-OP-{NN} 业务操作；AC-ERR-{NN} 异常场景；AC-NF-{NN} 非功能（不计入 e2e 分母）；UI-IXD-{NN} 交互类 UI 验收、UI-ELEM-{NN} 样式类 UI 验收（待原型产出后启用） |
| 覆盖说明 | 用户视角汇总（Phase 3）测试重点已并入：30 秒首条消息（AC-OP-01、AC-NF-07）、流式 2000+ token 不卡（AC-NF-02）、双向会话互操作（AC-FN-41、AC-OP-22）、审批记忆撤销即时生效（AC-OP-06）、压缩后 TODO 存活（AC-FN-33、AC-OP-14）、流式中 Steer 立即注入当前轮（AC-FN-52） |

---

## 1. 业务目标（G-{N}）

| 序号 | 业务目标 | 预期收益 | 来源 |
|------|---------|---------|------|
| G-1 | 已配置 Pi 的用户零配置接入，从打开插件到发出首条消息 ≤ 30 秒 | 零额外配置步骤，装完即用 | PRD 2.2, 2.3 |
| G-2 | 17 项核心路径功能全部可用，提供对标 Copilot/Claude Code 的完整 AI 编码体验 | 产品达到 Marketplace 发布水准 | PRD 2.2, 5.1 |
| G-3 | 与终端 Pi 生态完全同源：会话、模型、技能、凭证共用，双向续聊，token 与成本口径一致 | 零资产分裂 | PRD 2.2, 2.3 |
| G-4 | 全程不离开 VS Code 完成编码协作闭环（描述任务、流式执行、审批、改动落地、压缩、继续协作） | 主工作流不切换窗口 | PRD 3 |
| G-5 | 改动安全兜底：diff 审查、按轮次/步骤回滚、checkpoint 体系 | 误操作可恢复 | PRD 3 |
| G-6 | 深浅色主题与中英语言四组合无破版、无英文残留 | 产品化品质 | PRD 2.2 |
| G-7 | Marketplace 审核通过并成功发布 | 首发渠道可达 | PRD 2.2, 4 |
| G-8 | 既有能力零回归 | 升级不破坏现有用户 | PRD 4 |

### GOAL 追溯矩阵

| GOAL | 覆盖的 AC 条目 |
|------|----------------|
| G-1 | AC-FN-01~05, AC-OP-01, AC-NF-07 |
| G-2 | AC-FN-06~37, AC-FN-42~60, AC-OP-02~17, AC-OP-19, AC-OP-26~32, AC-NF-01~06, AC-NF-11~12, AC-NF-15, AC-RULE-35~36, AC-RULE-42~43 |
| G-3 | AC-FN-38~41, AC-FN-50, AC-OP-18, AC-OP-22, AC-ERR-04~05, AC-ERR-25, AC-RULE-27 |
| G-4 | AC-FN-12~15, AC-FN-17~24, AC-FN-34, AC-FN-37, AC-FN-52, AC-OP-02, AC-OP-03, AC-OP-05~07, AC-OP-11~17, AC-OP-29, AC-OP-30 |
| G-5 | AC-FN-10, AC-FN-26~28, AC-FN-46, AC-OP-03~04, AC-OP-08~13, AC-ERR-08~09, AC-ERR-12, AC-ERR-19~20, AC-ERR-24 |
| G-6 | AC-FN-36, AC-FN-42, AC-OP-20~21 |
| G-7 | AC-FN-42, AC-NF-08~10, AC-NF-13, AC-OP-23, AC-RULE-35~36 |
| G-8 | AC-NF-14, AC-OP-24~25 |

---

## 2. 验收标准（AC）

### 2.1 全局业务规则（AC-RULE-{NN}）

#### 状态枚举定义

| 编号 | 状态类型 | 状态值 | 语义说明 | UI 样式引用 | 来源 |
|------|---------|--------|---------|------------|------|
| AC-RULE-01 | Tab 会话状态 | 空闲 | 无进行中的请求；新建/加载会话或本轮完成、中断恢复后进入 | — | PRD 6.3 |
| AC-RULE-02 | Tab 会话状态 | 流式中 | 模型正在流式输出；发送消息后进入 | — | PRD 6.3 |
| AC-RULE-03 | Tab 会话状态 | 中断 | Esc、网络错误、限流或凭证失效（401/403）导致停止；已生成内容保留 | — | PRD 6.3 |
| AC-RULE-04 | Plan 模式状态 | 关闭 | 未启用 Plan；默认态，已完成/已中断后可进入 | — | PRD 6.3 |
| AC-RULE-05 | Plan 模式状态 | 规划中 | AI 只读分析产出计划 | — | PRD 6.3 |
| AC-RULE-06 | Plan 模式状态 | 待批准 | 计划卡片已呈现，等待用户决定 | — | PRD 6.3 |
| AC-RULE-07 | Plan 模式状态 | 执行中 | 按步骤自动执行，TODO 实时勾选 | — | PRD 6.3 |
| AC-RULE-08 | Plan 模式状态 | 已完成 | 全部步骤执行成功 | — | PRD 6.3 |
| AC-RULE-09 | Plan 模式状态 | 已中断 | Esc 停止，已完成步骤保留 | — | PRD 6.3 |
| AC-RULE-10 | 工具调用状态 | 待审批 | AI 发起工具调用等待决定 | — | PRD 6.3 |
| AC-RULE-11 | 工具调用状态 | 已放行 | 手动允许（可伴随写入记忆规则）、记忆命中且非危险工具（不经过待审批直接放行）、或 Plan 批准（计划内步骤且非危险工具）；三类均留痕 | — | PRD 6.3 |
| AC-RULE-12 | 工具调用状态 | 已拒绝 | 用户拒绝，结果回传 AI | — | PRD 6.3 |
| AC-RULE-13 | 工具调用状态 | 执行中 | 工具正在执行 | — | PRD 6.3 |
| AC-RULE-14 | 工具调用状态 | 完成 | 执行成功结束 | — | PRD 6.3 |
| AC-RULE-15 | 工具调用状态 | 失败 | 执行异常结束 | — | PRD 6.3 |
| AC-RULE-16 | 配置发现状态 | 未检测 | 插件激活后的初始状态 | — | PRD 6.3 |
| AC-RULE-17 | 配置发现状态 | 已发现 | 配置读取成功，UI 呈现 | — | PRD 6.3 |
| AC-RULE-18 | 配置发现状态 | 错误态 | 配置不存在、不可读（目录不存在、权限不足或被占用），或存在但无有效凭证/模型条目 | — | PRD 6.3 |
| AC-RULE-19 | 配置发现状态 | 刷新中 | 正在重读配置 | — | PRD 6.3 |
| AC-RULE-20 | 配置发现状态 | 部分可用 | 解析失败但保留旧列表 | — | PRD 6.3 |
| AC-RULE-21 | 计划步骤状态 | 待执行 | 步骤尚未开始 | — | PRD 6.2 |
| AC-RULE-22 | 计划步骤状态 | 执行中 | 步骤正在执行 | — | PRD 6.2 |
| AC-RULE-23 | 计划步骤状态 | 已完成 | 步骤执行成功 | — | PRD 6.2 |
| AC-RULE-24 | 计划步骤状态 | 失败 | 步骤执行失败 | — | PRD 6.2 |
| AC-RULE-25 | 计划步骤状态 | 已取消 | 中断后未执行步骤的终态 | — | PRD 6.2, 9.5 |
| AC-RULE-42 | Plan 模式状态 | 失败暂停 | 执行暂停在当前步骤（步骤失败/限流/凭证失效/危险工具或计划外工具待批或被拒/写入冲突待用户决定）；出口为修复后从当前步续跑、调整计划重新批准或放弃转已中断（写入冲突的用户决定映射到续跑与重新批准两类出口，见 AC-ERR-24） | — | PRD 6.3, 8.5 |
| AC-RULE-43 | Tab 外显状态 | 流式中 / Plan 执行中 / 失败暂停 | Tab 标题常驻状态图标的三种取值；非上述三种状态（空闲/已完成/已中断/关闭）不显示状态图标；失败暂停进入条件见 AC-RULE-42 | — | PRD 9.5 |

#### 计算与显示规则

| 编号 | 规则名称 | 规则内容 | 来源 |
|------|---------|---------|------|
| AC-RULE-26 | 配置统计口径 | 统计文案为"已从 Pi 读取 N provider / M 模型 / K 技能"；N = auth.json 中有效 provider 数，M = models.json 条目数（含自定义条目；自定义与内置 ID 冲突按 pi 合并语义去重后计数），K = 技能目录中可加载技能数 | PRD 8.3, 9.1, 11.1 |
| AC-RULE-27 | token 与成本口径 | 直接采用 pi 会话文件原生用量字段与 models.json 费率；插件与终端显示同值 | PRD 6.2, 9.1 |
| AC-RULE-28 | 费率缺失显示 | 模型费率元数据缺失时成本显示"未知费率"，禁止显示 0 | PRD 11.1 |
| AC-RULE-29 | 压缩阈值 | 上下文用量达到模型窗口 80%（默认阈值）时触发压缩提示条；用户关闭提示条后用量达到 90%（重新提示阈值）时提示条再次出现 | PRD 9.2 |
| AC-RULE-30 | 网络重试上限 | 同一请求自动重试上限 3 次，防止重试风暴 | PRD 11.2 |
| AC-RULE-31 | 操作防抖 | 5 秒内重复点击 Apply 或发送按钮时按钮置灰，静默防抖 | PRD 11.2 |

#### 机制与边界规则

| 编号 | 规则名称 | 规则内容 | 来源 |
|------|---------|---------|------|
| AC-RULE-32 | 危险工具例外 | 危险工具（任意 shell 命令执行）永不自动放行、禁止写入审批记忆；即使存在同类放行规则仍弹审批卡 | PRD 9.3, 11.5 |
| AC-RULE-33 | 审批记忆规则字段 | 字段为工具名、范围（本会话/全局两档）、创建时间；存扩展私有存储，不写入 `~/.pi/agent`；本会话档随 Tab 关闭自动清除，全局档持久保存直至撤销 | PRD 6.2 |
| AC-RULE-34 | 审批痕迹字段 | 字段为工具名、放行来源（手动/记忆命中/Plan 批准）、时间、参数摘要；消息流中可展开查看 | PRD 6.2 |
| AC-RULE-35 | 只读纪律 | 插件全生命周期（含崩溃、升级、多窗口）不写 `~/.pi/agent` 配置与技能文件（auth.json、models.json、skills/ 等）；会话文件按 pi 原生格式落盘属 pi 自身写入路径（AC-RULE-40），不属插件写入 | PRD 11.5 |
| AC-RULE-36 | 私有数据隔离 | 审批记忆与 checkpoint 存扩展自有存储；设置页标注存放位置并支持导出与清空 | PRD 11.5 |
| AC-RULE-37 | 偏好归属 | 默认模型、界面语言为 VS Code 侧设置，显式标注归属，永不回写 Pi 配置 | PRD 6.2, 9.6 |
| AC-RULE-38 | Plan 只读白名单 | Plan 规划中仅允许只读工具（读文件、搜索），任何写类工具调用被拒绝 | PRD 9.5 |
| AC-RULE-39 | Turn 序号 | Turn 在单 Tab 内单调递增；checkpoint 与 diff 共用同一序号 | PRD 1, 6.2 |
| AC-RULE-40 | 会话落盘 | 会话每轮即时落盘为 pi 原生格式，无写缓冲延迟，防交替写入覆盖 | PRD 8.3, 11.5 |

#### 权限模型

| 编号 | 角色 | 可访问功能 | 数据范围 | 角色特有行为 | 来源 |
|------|------|-----------|---------|-------------|------|
| AC-RULE-41 | 本机开发者（VS Code 启动用户） | 全部功能 | 本机全部数据 | 唯一角色，单用户本机扩展，无多角色体系；Pi 配置（凭证/模型）仅可查看，插件不写入 | PRD 12.1 |

### 2.2 功能验收标准（AC-FN-{NN}）

#### 2.2.1 配置接入（核心路径 1）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-01 | `~/.pi/agent` 含有效配置（≥1 个 provider 凭证与模型条目，models.json 含自定义模型条目） | 打开插件面板 | 1) 面板显示统计文案"已从 Pi 读取 N provider / M 模型 / K 技能"（N、M、K 按 AC-RULE-26 计算） 2) 模型选择器列表与终端 `/model` 呈现一致（含自定义条目；ID 冲突按 pi 合并语义仅呈现一条） 3) 技能菜单与会话历史列表呈现对应条目 4) 从打开面板到输入区可发送消息之间无任何配置操作 | PRD 9.1, 8.3 |
| AC-FN-02 | `~/.pi/agent` 目录不存在、不可读，或存在但无有效凭证/模型条目 | 打开插件面板 | 1) 面板呈现错误态卡片，包含缺失说明（区分"目录不存在/不可读"与"存在但无有效凭证或模型条目"）、文档链接、"重新检测"按钮 2) 不出现空白面板 3) 不出现配置表单 4) 扩展进程不崩溃 | PRD 9.1, 11.3 |
| AC-FN-03 | 面板处于错误态；用户已在终端完成 Pi 配置；输入框中存在未发送草稿 | 点击"重新检测"按钮 | 1) 按钮进入加载状态且不可重复点击 2) 检测成功后面板转入已发现态并渲染模型列表（AC-RULE-17） 3) 全程无需重启 VS Code 4) 输入框草稿保留原内容 5) 检测仍失败时面板维持错误态并刷新具体原因（区分仍不存在/权限不足），草稿保留，可再次重试 | PRD 9.1 |
| AC-FN-04 | 终端环境可发现 pi 凭证；当前 VS Code 进程读取不到同等凭证（GUI 启动未继承 shell 环境变量） | 打开插件面板 | 面板显示具体原因"凭证依赖的环境变量在当前 VS Code 进程不可见（桌面启动不继承 shell 变量）"，并给出将 key 落到 auth.json 的建议动作；不显示笼统的"无可用模型"文案 | PRD 9.1, 11.4 |
| AC-FN-05 | pi 会话目录存在历史会话（含终端 CLI 产生的会话） | 打开会话历史入口，搜索目标会话并加载 | 1) 会话列表按项目与时间分组展示，搜索框可用 2) 加载后消息上下文完整呈现 3) token 用量与终端侧显示同值（口径按 AC-RULE-27） 4) 新消息的 Turn 序号延续原会话最大值递增（AC-RULE-39） 5) 会话含不完整轮次时该轮标注为不完整/待补全并提供"重试补全"入口（AC-ERR-22）；补全后该轮在会话文件中替换为完整轮次，不留损坏记录 | PRD 9.1, 8.3 |
| AC-FN-57 | 插件已完成配置发现（AC-FN-01） | 点击"新建 Tab" | 1) 新空 Tab 呈现且输入区可用 2) 会话消息从 Turn 1 起算（AC-RULE-39） 3) 会话文件在首轮完成时创建并按 pi 原生格式落盘（AC-RULE-40） | PRD 9.1 |
| AC-FN-06 | 插件已加载旧配置；外部已修改 models.json | 打开模型选择器 | 1) 选择器重读配置，列表反映最新配置内容且新条目可选 2) 全程无需重启 VS Code 3) 重读过程中呈现刷新中过渡指示（AC-RULE-19），成功后回到已发现（AC-RULE-17） 4) 刷新时配置不存在/不可读或无有效凭证则转入错误态（AC-RULE-18，与 AC-FN-02 同口径呈现），出口经 AC-FN-03 重新检测 5) 进行中会话所用模型被删除的场景见 AC-FN-49 | PRD 9.1 |
| AC-FN-49 | 插件已加载旧配置；某进行中会话使用的模型已从配置中删除 | 打开模型选择器 | 1) 显示更换模型提示 2) 会话可改选其他模型继续对话，不中断 | PRD 9.1 |
| AC-FN-44 | `~/.pi/agent` 技能目录含若干技能文件 | 打开输入区技能菜单并选中其中一个技能 | 1) 技能菜单条目与终端 `/` 菜单一致（名称与描述同源） 2) 选中后技能内容作为指令上下文进入当前会话，调用效果与终端侧一致 3) 菜单支持关键词过滤 | PRD 9.1, 8.3 |
| AC-FN-45 | 插件已加载技能列表；外部在技能目录新增/删除一个技能文件 | 重新打开技能菜单 | 1) 菜单反映最新技能清单，无需重启 VS Code 2) 被删除的技能不再出现 | PRD 9.1 |

#### 2.2.2 图片输入（核心路径 2）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-07 | 当前模型支持图片输入 | 分别以粘贴到输入区、点击图片按钮选择、拖拽图片文件到输入区三种方式添加图片并发送 | 1) 添加后图片以缩略图形式出现在输入区待发送列表 2) 发送后图片随消息出现在消息流 3) 图片数据随该轮消息按 pi 会话格式落盘 | PRD 9.2 |
| AC-FN-08 | 当前模型不支持图片输入 | 点击图片按钮；随后粘贴一张图片到输入区 | 1) 图片按钮呈置灰状态 2) 粘贴图片时显示提示，内容包含当前模型不支持图片输入的说明 3) 点击置灰的图片按钮时同样给出当前模型不支持图片输入的提示 4) 图片不被添加到待发送列表（不发生无声失败） | PRD 9.2 |

#### 2.2.3 代码块渲染与 Apply（核心路径 3）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-09 | 消息流中存在带语言标注的代码块 | 无额外操作（消息渲染完成后验证） | 1) 代码块按其语言着色高亮 2) 代码块 ≥ 20 行时显示折叠控件，折叠后保留首行与展开按钮 3) 渲染过程为增量更新，不整块重绘（联动 AC-NF-02） | PRD 9.3 |
| AC-FN-10 | 消息流中存在代码块 | 点击该代码块的 Apply 按钮，查看预览后点击确认 | 1) 点击 Apply 后先显示目标文件选择与将替换内容的 diff 预览，不直接写入 2) 确认后写入目标文件，消息流中该操作显示完成状态 3) 写入的改动纳入本 Tab checkpoint 体系，可通过一步撤销恢复到写入前内容（流程见 AC-OP-11） 4) 预览与确认之间文件被外部修改的冲突场景见 AC-ERR-09 | PRD 9.3, 11.2 |
| AC-FN-11 | 消息流中存在代码块 | 点击该代码块的复制按钮 | 剪贴板内容为代码块原始文本（未经渲染处理的原文） | PRD 9.3 |

#### 2.2.4 消息队列（核心路径 4 关联）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-12 | Tab 处于流式中 | 在流式完成前依次发送两条新消息 | 1) 两条消息依次进入队列并显示排队状态 2) 队列消息依序发送与落盘断言见 AC-OP-29 3) 发生网络重试时队列消息不重复发送（联动 AC-ERR-07） | PRD 8.5, 11.2 |
| AC-FN-52 | Tab 处于流式中 | 通过输入区 Steer 按钮/快捷键发送引导消息 | 1) 消息立即注入当前轮，不进入 FollowUp 队列 2) 消息流可见插入标识，本轮后续回复体现该引导 3) Tab 保持流式中（AC-RULE-02） 4) 消息随本轮按 pi 原生格式落盘（AC-RULE-40） | PRD M06, 8.5 |

#### 2.2.5 工具审批与记忆（核心路径 5）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-13 | AI 发起一次工具调用且审批记忆未命中 | 无额外操作（审批卡片呈现即验证） | 1) 卡片展示工具名与参数摘要 2) 危险工具额外显示危险标识 3) 卡片提供"允许一次""允许并记住（本会话）""允许并记住（全局）""拒绝"四种操作 | PRD 9.3 |
| AC-FN-14 | 已存在某非危险工具的全局放行规则 | AI 发起危险工具（任意 shell 命令执行）调用 | 1) 仍弹出审批卡片，不自动放行 2) 该卡片上"允许并记住"操作不可用（危险工具禁止写入审批记忆，AC-RULE-32） | PRD 9.3, 11.5 |
| AC-FN-15 | 消息流中同时存在记忆命中放行与手动批准的工具调用各一次 | 分别展开两者的痕迹条目 | 1) 两者均为可展开条目，展开后显示工具名、放行来源、时间、参数摘要（字段按 AC-RULE-34） 2) 记忆命中条目的放行来源显示为"记忆"，手动批准条目显示为"手动"，Plan 批准放行条目显示为"Plan 批准"（放行来源字段文案可区分，AC-RULE-34） | PRD 9.3 |
| AC-FN-16 | 设置页存在审批记忆规则 | 打开设置页审批记忆区域 | 1) 列表逐条显示工具名、范围（本会话/全局）、创建时间 2) 页面显示数据边界说明：审批记忆与 checkpoint 存于扩展自有存储，不写入 `~/.pi/agent` 3) 提供逐条撤销操作与一键清空操作 | PRD 9.6 |

#### 2.2.6 上下文压缩（核心路径 6）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-17 | 当前 Tab 上下文用量达到阈值（窗口 80%，AC-RULE-29）；会话中存在进行中计划与 TODO 卡片 | 无额外操作（提示条出现即验证） | 1) 提示条说明压缩摘要将覆盖的范围 2) 保留清单中列出计划、TODO 与未发送的 FollowUp 队列消息 3) 提示条提供"压缩"与"暂不"两个按钮 | PRD 9.2 |
| AC-FN-18 | 压缩提示条已出现且用户已点击过"暂不" | 在同一阈值区间内继续对话 | 提示条不再重复弹出；上下文用量达到 90%（重新提示阈值，AC-RULE-29）后提示条重新出现 | PRD 9.2, 8.3 |
| AC-FN-19 | 任一 Tab 处于空闲状态 | 执行手动压缩命令 `/compact` | 1) 触发 Pi 原生压缩流程（与提示条入口同一机制） 2) 压缩完成后计划与 TODO 仍可见，未发送的 FollowUp 队列消息保留且依序发送语义不变（与 AC-OP-29 一致） 3) 上下文用量回落；压缩产物落盘与统计连续性断言见 AC-OP-14、AC-OP-30 | PRD 9.2 |

#### 2.2.7 编辑器交互（核心路径 7、8、9）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-20 | 编辑器中已选中代码 | 右键点击选区，在菜单中点击"发送到 Pi" | 1) 选区代码、文件路径、行号作为上下文发送 2) 存在当前 Tab 时进入当前 Tab，无 Tab 时新建 Tab 3) 目标 Tab 输入区可见该上下文引用 4) 目标 Tab 处于流式中时，该上下文按普通发送进入 FollowUp 队列（联动 AC-FN-12/AC-OP-29） | PRD 9.4 |
| AC-FN-21 | 聊天输入区已获得焦点；工作区存在文件与符号索引 | 在输入区输入 @ 字符并继续输入关键词，在补全面板中选中一条文件或符号条目 | 1) 补全面板对文件名与符号名做模糊匹配并展示候选 2) 选中项作为引用注入上下文，输入区显示该引用标记 3) 发送后引用内容随消息提供给模型 | PRD 9.2 |
| AC-FN-22 | 工作区无符号索引 | 在输入区输入 @ 字符 | 补全面板降级为仅展示文件级条目，不显示符号条目；选中文件后仍可完成引用注入 | PRD 9.2 |
| AC-FN-23 | 输入区已添加某文件引用且消息尚未发送；该文件已被删除 | 点击发送 | 提示该引用文件已不存在，消息内容中不含该失效引用 | PRD 9.2 |
| AC-FN-24 | 资源管理器中存在文件 | 从资源管理器按住文件拖动到聊天对话区域并释放鼠标 | 该文件转为文件引用（走与 @-mention 相同的注入路径），输入区显示该引用标记 | PRD 9.2 |

#### 2.2.8 Inline Chat（核心路径 10）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-25 | 编辑器中已选中代码 | 选中编辑器内代码后按呼出快捷键 | 1) 编辑器内出现行内输入框并获得焦点 2) 提交指令后的生成与呈现见 AC-FN-26 | PRD 9.4 |
| AC-FN-26 | Inline Chat 已生成结果且尚未落盘 | 无额外操作（生成完成即验证） | 1) 生成结果以行内 diff 呈现，文件实际内容未变（未落盘） 2) diff 区域超过 40 行或 3 个差异块时提供折叠与跳转控件 3) diff 呈现不阻塞编辑器滚动与导航 | PRD 9.4 |
| AC-FN-27 | 文件存在未保存的手动修改；Inline Chat 针对该文件生成完成 | 点击"接受"，在冲突提示中分别选择两个操作 | 1) 先显示冲突提示，提供"以生成结果覆盖"与"保留手改放弃生成"两个操作，不静默混合 2) 覆盖：按 AC-OP-12 语义应用生成结果并纳入 checkpoint 3) 放弃：文件保持手改内容，按 AC-OP-13 语义行内 diff 收起 | PRD 9.4 |
| AC-FN-28 | Inline Chat 正在生成；生成期间用户手动编辑了同一文件 | 生成完成并尝试应用结果 | 应用前提示"文件已变化"，由用户决定继续应用或放弃；不静默覆盖手动修改 | PRD 9.4 |
| AC-FN-56 | Inline Chat 生成过程中 AI 发起中间工具调用（读文件/搜索/写文件等） | 查看审批行为 | 1) 按所属 Tab 审批体系处理：审批记忆命中自动放行并留痕（AC-RULE-34） 2) 未命中弹出审批卡（AC-FN-13） 3) 危险工具仍弹卡不放行（AC-RULE-32） 4) 拒绝时生成中止，文件保持原状（AC-ERR-23 口径） | PRD 9.4, 9.3 |

#### 2.2.9 Plan 模式（核心路径 11）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-29 | Tab 处于对话模式 | 通过模式切换器切到 Plan | 1) 输入区标注当前模式为 Plan 2) 本轮 AI 仅可调用只读工具，任何写类工具调用被拒绝（AC-RULE-38） 3) Tab 转入规划中（AC-RULE-05） | PRD 9.2, 9.5 |
| AC-FN-51 | Tab 已切换至 Plan 模式（AC-FN-29） | 发送任务描述消息 | 1) 本轮 AI 仅调用只读工具（AC-RULE-38） 2) AI 产出计划并以分步 TODO 计划卡片呈现 3) Tab 转入待批准状态（AC-RULE-06） 4) 本轮会话按 pi 原生格式落盘（AC-RULE-40） | PRD 8.4, 9.5, 6.3 |
| AC-FN-30 | 计划卡片处于待批准状态（AC-RULE-06） | 调整步骤顺序、删减步骤后点击批准 | 1) 计划卡片按调整后内容生效 2) Tab 转入执行中状态（AC-RULE-07） 3) 按调整后的步骤序列自动执行 | PRD 9.5 |
| AC-FN-31 | Plan 执行中某步骤执行失败 | 无额外操作（失败标注呈现即验证） | 1) 失败步骤标注失败状态（AC-RULE-24），已完成步骤保持已完成标注（AC-RULE-23） 2) Tab 级归宿：转入失败暂停（AC-RULE-42），出口经 AC-OP-28（续跑/重新批准/放弃） | PRD 9.3, 9.5 |
| AC-FN-32 | 三个 Tab 分别处于流式中、Plan 执行中、失败暂停 | 查看 Tab 标题栏 | 1) 每个 Tab 标题显示常驻状态图标，三者图标互不相同 2) 处于后台的 Tab 失败时发出 VS Code 通知 3) 图标枚举与 AC-RULE-43 一致 | PRD 9.5, 9.8 |
| AC-FN-43 | Plan 执行中某步骤发起一次危险工具调用（如 shell 命令执行） | 查看审批卡片与 Tab 状态 | 1) 弹出审批卡片且 Tab 转入失败暂停（AC-RULE-42），执行不继续 2) 批准后从当前步骤续跑，消息流产生放行来源=手动的可展开痕迹（AC-RULE-34） 3) 拒绝后该步骤标记失败（AC-RULE-24）并维持失败暂停，可调整计划重新批准或放弃（AC-OP-28）；不把失败当完成 | PRD 9.5 |
| AC-FN-48 | Plan 已完成或已中断 | 通过模式切换器切回对话模式 | 1) Plan 标注清除，输入区恢复对话模式 2) 执行期改动与 checkpoint 保留（AC-OP-03） 3) Tab 状态流转与 6.3 状态机一致（关闭态） | PRD 9.5 |
| AC-FN-54 | 计划卡片处于待批准状态 | 请求 AI 重规划 | 1) Tab 退回规划中（AC-RULE-05）重新产出计划 2) 原计划标注被替换 | PRD 9.5, 6.3 |
| AC-FN-55 | Plan 处于规划中或待批准状态 | 通过模式切换器切回对话模式或取消 | 1) Plan 标注清除，Tab 转入关闭态（AC-RULE-04） 2) 已产生的只读分析轮次落盘保留（AC-RULE-40） | PRD 6.3, 8.5 |
| AC-FN-58 | Plan 执行中，AI 发起计划外非危险且审批记忆未命中的工具调用 | 查看审批卡片与步骤状态，点击放行或拒绝 | 1) 弹出审批卡，弹卡期间该步骤暂停（失败暂停态，AC-RULE-42） 2) 放行后该步骤继续执行，消息流产生放行来源=手动的可展开痕迹（AC-RULE-34） 3) 拒绝后该步骤标记失败（AC-RULE-24）并维持失败暂停，可调整计划重新批准或放弃（AC-OP-28），不把失败当完成 | PRD 9.5 |
| AC-FN-59 | Tab 处于 Plan 模式（规划中/待批准/执行中/失败暂停任一状态） | 关闭该 Tab | 1) 规划中/待批准：Plan 直接终止，无残留待批准卡片 2) 执行中/失败暂停：视同中断（AC-RULE-09），已完成步骤的改动与 TODO 标注保留，未执行步骤已取消（AC-RULE-25） 3) 会话已落盘轮次保留（AC-RULE-40） | PRD 8.5, 6.3 |
| AC-FN-60 | 插件已发现配置且工作区已打开 | 依次完成一次完整协作：描述问题→AI 修改文件→审查 diff→运行测试→生成 commit message | 1) 全流程在 VS Code 内完成，无需切换外部工具 2) 各环节分别满足 AC-FN-12（流式）、AC-OP-03/11（审批与 Apply）、AC-FN-34（终端）、AC-OP-15（commit message）断言 3) 全程改动纳入本 Tab diff/checkpoint 体系，可按轮次回滚 | PRD 14, 2.3 |

#### 2.2.10 TODO 卡片（核心路径 12）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-33 | 会话已完成一次压缩；压缩前存在计划与 TODO 卡片 | 无额外操作（压缩完成后验证） | 计划本体与 TODO 卡片仍然可见，内容与压缩前一致，未因压缩丢失 | PRD 9.3 |

#### 2.2.11 终端集成与后台任务（核心路径 13、15）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-34 | 集成终端存在输出内容 | AI 执行需要终端信息的任务 | 1) AI 回复中引用了终端输出内容 2) 用户手动复制终端内容粘贴进对话后，该内容随消息发送成功 | PRD 9.8 |
| AC-FN-35 | 存在后台任务（运行中、完成、失败三种状态各一条） | 打开后台任务面板 | 1) 列表逐条显示任务及其状态 2) 运行中任务显示取消操作，取消后状态变更为已取消（流程见 AC-OP-17） | PRD 9.8 |

#### 2.2.12 设置、底座与会话互操作（核心路径 14、16、17 及横切项）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-FN-36 | 插件处于中文界面 | 在设置页将界面语言切换为 English | 1) 全部界面文案（含动态拼接与变量替换文案）在不重启 VS Code 的条件下更新为英文 2) 切回中文同样即时生效 | PRD 9.6, 13.4 |
| AC-FN-37 | 插件任意界面可用 | 依次按发送、审批通过、审批拒绝、接受 diff、放弃 diff、切 Tab、切模型、会话切换、聚焦输入框对应的快捷键 | 全部键位生效；主操作显示键位提示；键位可在 VS Code 键位设置中自定义 | PRD 13.4 |
| AC-FN-38 | 某会话正被终端 CLI 写入 | 在插件中加载该会话 | 1) 会话以只读方式打开并显示提示"该会话正在其他地方使用，已只读打开" 2) 输入区不可用（置灰）并显示占用提示 3) 插件侧不产生写入 4) 占用解除后占用提示更新并提供"恢复可写"操作；只读态提示区提供"接管"动作（复用 AC-OP-18 接管语义） | PRD 11.3 |
| AC-FN-39 | 某会话已被第一个 VS Code 窗口打开 | 在第二个 VS Code 窗口加载同一会话 | 1) 弹出提示"已在其他窗口打开" 2) 提供"只读打开"与"接管"两个操作，强制二选一（接管流程见 AC-OP-18） 3) 选择"只读打开"后输入区置灰，提示区提供"接管"动作（AC-OP-18） | PRD 9.8 |
| AC-FN-40 | 用户从历史列表加载一条旧会话 | 在该会话中发送新消息 | 1) 追加前显示警告"将追加到历史会话（该会话终端侧同样可见）" 2) 确认后消息追加成功并落盘 3) 选择新建会话的分支见 AC-FN-50 | PRD 11.2 |
| AC-FN-41 | 终端 CLI 已产生含历史轮次的会话 | 在插件中加载该会话并继续对话，随后在终端执行 `pi --continue` | 1) 终端读回全部轮次，包含插件中新增的轮次 2) Turn 序号延续递增无重复 3) token 与成本两侧显示同值（AC-RULE-27） | PRD 9.1, 14 |
| AC-FN-42 | 界面与图标整体重设计已应用 | 对照第 4 节附录逐项打开 17 项核心路径功能的入口 | 1) 每项功能入口可达，无入口丢失 2) 扩展图标更新，包含 marketplace 彩色图标与 36x36 灰阶规范图标 3) diff、checkpoint、会话管理的既有入口行为不变 | PRD 5.1, 16.2; proposal.md |
| AC-FN-46 | 某轮次产生了工具改动（含多个文件） | 打开该 Tab 的变更清单，点击该轮次条目查看 diff | 1) 可查看该轮次全部文件级 diff 2) 可从条目直接发起回滚（流程见 AC-OP-04） 3) 变更清单按轮次/步骤两级粒度可查 | PRD 9.3 |
| AC-FN-47 | 设置页存在默认模型设置项 | 查看该设置项与 Pi 侧配置的关系 | 1) 该设置项显式标注归属"VS Code 侧"（AC-RULE-37） 2) 与 Pi 侧配置不一致时说明以插件设置为准并可解释差异 3) 修改该设置后 `~/.pi/agent` 无新增写入 | PRD 9.6 |
| AC-FN-53 | 模型选择器可用 | 在模型选择器中设置思考级别 | 1) 档位枚举为 off/minimal/low/medium/high 2) 设置对当前会话级生效 3) VS Code 侧不另存该偏好，跟随 Pi SDK 原生会话级机制（AC-RULE-37 口径） | PRD M06, 6.2 |
| AC-FN-50 | 用户从历史列表加载一条旧会话且弹出追加警告 | 在警告中选择"新建会话" | 1) 原会话内容不变且未被写入 2) 新 Tab 呈现空会话，消息从 Turn 1 重新计数 | PRD 11.2 |

### 2.3 业务操作标准（AC-OP-{NN}）

| 编号 | Given（前置条件） | When（操作） | Then（预期结果） | 来源 |
|------|------------------|-------------|-----------------|------|
| AC-OP-01 | 已配置 Pi 的环境新安装并启用插件 | 打开面板、选择模型、发送首条消息 | 1) 面板打开后自动完成配置发现并显示统计文案 2) 选择模型后发送消息，本轮及后续对话使用所选模型，回复流式呈现至本轮完成 3) 从打开面板到发出首条消息全程无配置操作，耗时 ≤ 30 秒 | PRD 2.2, 2.3 |
| AC-OP-02 | Tab 处于空闲状态 | 发送一条消息并等待本轮完成 | 1) 发送后 Tab 状态转入流式中（AC-RULE-02） 2) 本轮完成后转回空闲（AC-RULE-01） 3) 会话文件在本轮完成时已包含该轮全部消息（每轮即时落盘，AC-RULE-40） 4) Turn 序号递增 1（AC-RULE-39） 5) 5 秒内重复点击发送按钮时按钮置灰防抖（AC-RULE-31） | PRD 8.3, 11.5 |
| AC-OP-03 | 某工具调用已被放行 | 工具执行完成并修改文件 | 1) 修改进入本 Tab 的 diff 记录 2) 生成本轮次 checkpoint（文件前后快照） 3) 执行结果回传 AI，对话继续 | PRD 8.3 |
| AC-OP-04 | 某轮工具改动已生成 checkpoint | 在变更清单中点击该轮次条目的回滚操作并确认 | 1) 该轮改动的文件恢复到该轮执行前内容 2) 其他轮次的改动与 redo 链不受影响 3) 消息流中回滚结果可见 | PRD 6.2, 9.5 |
| AC-OP-05 | 某非危险工具首次被调用并弹出审批卡 | 点击"允许并记住"并选择全局 | 1) 规则（工具名、范围=全局、创建时间）写入扩展私有存储（AC-RULE-33） 2) 本次调用放行并执行 3) 此后任意 Tab 的同类调用自动放行 4) 每次自动放行在消息流产生可展开痕迹条目，放行来源显示为"记忆"（AC-RULE-34） | PRD 9.3 |
| AC-OP-31 | 审批卡片已呈现（审批记忆未命中且非危险工具） | 点击"允许一次" | 1) 本次调用放行并执行（AC-RULE-11→13→14） 2) 不写入任何记忆规则 3) 同类工具再次调用时重新弹出审批卡 4) 消息流产生放行来源=手动的可展开痕迹（AC-RULE-34） | PRD 9.3, 8.2 |
| AC-OP-32 | 审批卡片已呈现（审批记忆未命中且非危险工具） | 点击"允许并记住"并选择本会话 | 1) 规则（工具名、范围=本会话、创建时间）写入扩展私有存储（AC-RULE-33） 2) 本次调用放行并执行 3) 本 Tab 内同类调用自动放行并留痕（放行来源=记忆，AC-RULE-34） 4) 仅本 Tab 生效，其他 Tab 同类调用仍弹卡（联动 AC-OP-26） | PRD 9.3 |
| AC-OP-06 | 设置页存在至少一条审批记忆规则 | 撤销其中一条 | 1) 列表移除该条 2) 该撤销对所有 Tab 生效 3) 下一次同类工具调用重新弹出审批卡，不自动放行 | PRD 9.6 |
| AC-OP-07 | 设置页存在多条审批记忆规则 | 点击一键清空 | 1) 全部规则移除 2) 此后所有工具调用回到逐次弹审批卡 | PRD 9.6 |
| AC-OP-08 | 计划卡片处于待批准状态 | 点击批准 | 1) Tab 转入执行中（AC-RULE-07） 2) 步骤按序连续执行，TODO 卡片随每步完成实时勾选 3) 全部完成后 Tab 转入已完成（AC-RULE-08），本轮全部执行改动进入 diff 与 checkpoint 4) 执行中计划内非危险工具调用自动放行（凭据=计划批准），消息流产生放行来源=Plan 批准的可展开痕迹（AC-RULE-34）；危险工具例外仍弹审批卡（AC-FN-43） | PRD 8.4, 9.5 |
| AC-OP-09 | Plan 执行中且已完成部分步骤 | 按 Esc | 1) 当前步骤与后续步骤停止执行 2) 已完成步骤的改动保留，变更清单按步骤可查 3) 未执行步骤状态变更为已取消（AC-RULE-25） 4) Tab 转入已中断（AC-RULE-09） 5) 调整计划后 Tab 转入规划中（AC-RULE-05），重新产出计划并走待批准→执行中流程（AC-OP-08） | PRD 8.4, 9.5 |
| AC-OP-10 | Plan 执行产生多步骤改动 | 在变更清单中点击该步骤条目的回滚操作并确认 | 1) 该步骤改动恢复到该步骤执行前内容 2) 其他步骤与轮次的改动及其 redo 链不受影响 3) 消息流中回滚结果可见，会话可继续对话 | PRD 9.5 |
| AC-OP-11 | 代码块 Apply 已弹出 diff 预览且目标文件未被外部修改 | 点击确认写入，随后点击一步撤销 | 1) 确认后目标文件内容更新为 diff 预览所示内容 2) 改动纳入本 Tab checkpoint 3) 撤销后文件恢复到写入前内容 | PRD 9.3 |
| AC-OP-12 | Inline Chat 行内 diff 已生成且无冲突提示 | 点击接受 | 1) 文件更新为行内 diff 所示内容 2) 改动纳入本 Tab 的 diff 与 checkpoint 体系（可回滚） 3) 行内 diff 控件收起 4) 本轮 Inline Chat 对话与结果计入所属 Tab 会话，Turn 序号递增并按 pi 原生格式落盘（AC-RULE-39/40）；无活跃 Tab 时新建 Tab 承接 | PRD 9.4 |
| AC-OP-13 | Inline Chat 经多轮追加指令累计产生修改（未落盘的生成结果 + 本轮已落盘的工具写入） | 点击放弃 | 1) 未落盘的生成结果仅作废不写入 2) 本轮内已经由中间工具调用写入的文件改动，按该 Tab 本轮 checkpoint 自动回滚到轮前状态 3) 行内 diff 消失 | PRD 9.4 |
| AC-OP-14 | 压缩提示条已出现 | 点击"压缩" | 1) 提示条进入执行中状态 2) 压缩经 Pi 原生机制完成 3) 完成后计划与 TODO 仍可见，未发送的 FollowUp 队列消息保留并按入队顺序依序发送（AC-OP-29 语义不变） 4) 上下文用量回落到阈值以下 5) 会话可继续对话 6) 压缩发生于 Plan 执行中时，执行在压缩完成后继续，已完成步骤与 TODO 勾选状态保留（与 AC-ERR-13 失败路径口径一致） | PRD 9.2, 8.3 |
| AC-OP-15 | 工作区存在未提交改动 | 点击 SCM 输入框旁"生成 commit message"按钮 | 1) 按钮进入生成中状态 2) 生成文案填入 SCM 输入框 3) 不触发提交动作 4) 再次点击可重新生成并覆盖输入框内容；用户可编辑后自行提交 | PRD 9.7 |
| AC-OP-16 | 流式已被中断（Esc/网络错误/限流/凭证失效，AC-RULE-03），已生成内容保留 | 分别走两条路径：点击重试并等待本轮完成 / 直接发送新消息 | 1) 重试路径：Tab 由中断转入流式中（AC-RULE-02），本轮以同轮续跑方式补全，Turn 序号不变，完成后该轮在会话文件中替换为完整轮次、不重复入历史（与 AC-ERR-22/AC-FN-05 口径一致），Tab 转回空闲（AC-RULE-01） 2) 直接发送新消息路径：视为放弃补全，原不完整轮次定格保留，新消息开启新一轮，Turn 序号递增、无重复序号 3) 两条路径均保留展示已生成内容 | PRD 8.5, 11.2, 8.3 |
| AC-OP-17 | 后台任务面板存在一条运行中任务，该任务对应处于 Plan 执行中（图标=Plan 执行中）的 Tab | 点击该任务的取消操作 | 1) 任务状态变更为已取消 2) 若该任务是 Plan 执行载体：取消即中断该 Plan 执行，Tab 转入已中断（AC-RULE-09，联动 AC-OP-09），未执行步骤已取消（AC-RULE-25），Tab 标题状态图标清除（AC-RULE-43 非活动态无图标） 3) 若该任务仅为子代理任务：Plan 执行不受影响，Tab 标题状态图标保持 Plan 执行中（AC-RULE-43） | PRD 9.8 |
| AC-OP-18 | 第二窗口已弹出"已在其他窗口打开"提示 | 点击"接管" | 1) 本窗口获得该会话写入权 2) 同一时刻该会话仅存在一个写入方，原窗口转为只读 3) 接管后输入区恢复可写 | PRD 9.8 |
| AC-OP-19 | 同一窗口中存在 Tab A 与 Tab B | 在 Tab A 发送消息并产生工具改动 | Tab B 的会话、流式状态、diff 与 checkpoint 均不发生变化（状态不跨 Tab 共享） | PRD 6.2 |
| AC-OP-20 | 用户已将界面语言切换为 English | 重启 VS Code 后打开插件 | 1) 界面仍为英文 2) 该偏好存于 VS Code 侧设置并显式标注归属；`~/.pi/agent` 无新增写入（AC-RULE-37） | PRD 9.6 |
| AC-OP-21 | 插件支持深浅色主题与中英语言；已遍历主题与语言的四组合，逐一访问全部功能界面（含动态拼接文案界面） | 无额外操作（遍历完成后验证） | 1) 中文环境下全部界面无英文残留 2) 四组合均无破版、无文字截断、无控件重叠 | PRD 2.2, 13.3 |
| AC-OP-22 | 同一会话被终端 CLI 与插件先后交替写入（双方均通过占用检测，AC-OP-18） | 双方各写入一轮消息 | 1) 两个轮次均落盘，历史无覆盖丢失（每轮即时落盘，AC-RULE-40） 2) 一侧检测到并发写入时呈现占用与只读提示（AC-FN-38） | PRD 11.3, 11.5 |
| AC-OP-23 | 扩展已积累审批记忆与 checkpoint 私有数据 | 在设置页执行导出，随后执行清空 | 1) 导出生成包含审批记忆与 checkpoint 的数据文件 2) 清空后审批记忆列表为空，checkpoint 数据移除 3) `~/.pi/agent` 内容不受影响 | PRD 11.5 |
| AC-OP-24 | 旧版本插件存在设置键与私有数据 | 升级安装新版本 | 1) 既有扩展设置键全部保留 2) 审批记忆初始化为空 3) 既有会话与 checkpoint 数据格式兼容，内容不迁移不变质 | PRD 13.5 |
| AC-OP-25 | 新版本已安装且产生私有数据 | 回退安装旧版本 | 1) 旧版本可读取新版本私有数据；对旧版本无法读取的私有数据项逐项判定：可读取项正常呈现，不可读取项逐项显示迁移提示（含数据项名称与建议动作）；两种情形下均不发生静默失效 2) 会话数据按 pi 原生格式在终端与旧版本中可继续读取 | PRD 13.5 |
| AC-OP-26 | Tab A 已通过"允许并记住（本会话）"建立某工具的本会话档规则；Tab B 为另一会话的 Tab | 在 Tab B 发起同类工具调用；随后在 Tab A 内再次发起同类调用 | 1) Tab B 的同类调用仍弹出审批卡，不自动放行 2) Tab A 内的同类调用自动放行，消息流留痕且放行来源显示为"记忆"（AC-RULE-34） | PRD 9.3 |
| AC-OP-27 | Tab A 存在本会话档审批规则；全局档另存在一条不同工具的规则 | 关闭 Tab A 后新建 Tab | 1) 本会话档规则消失，同类调用重新弹审批卡 2) 全局档规则仍生效，其同类调用自动放行并留痕 | PRD 9.6 |
| AC-OP-28 | Plan 执行处于失败暂停（AC-RULE-42）；已完成步骤保留改动与 TODO 标注 | 分别走三条路径：修复问题后点击继续 / 调整计划重新批准 / 放弃 | 1) 继续路径：从当前失败步骤续跑，已完成步骤的改动与 TODO 标注不变 2) 重新批准路径：Tab 转入待批准（AC-RULE-06），批准后按调整后计划执行（AC-OP-08） 3) 放弃路径：Tab 转入已中断（AC-RULE-09），未执行步骤已取消（AC-RULE-25） 4) 三条路径均不把失败步骤标记为完成 | PRD 9.5 |
| AC-OP-29 | Tab 流式中，FollowUp 队列已有两条消息 | 等待当前轮完成 | 1) 当前轮结束后两条消息按入队顺序依序发送 2) 每条消息发送后作为独立轮次按 pi 原生格式落盘（AC-RULE-40） 3) 队列发送过程中 Tab 状态流转与 8.5 状态机一致 | PRD 8.5 |
| AC-OP-30 | 会话已完成一次压缩；压缩前后的 token 用量与成本有记录 | 对比压缩前最后一次统计与压缩后首次统计 | 1) token 用量与成本统计连续可对账，无重复计费或缺口 2) 口径与终端侧一致（AC-RULE-27） | PRD 9.2 |

### 2.4 异常场景（AC-ERR-{NN}）

| 编号 | 异常场景 | 系统行为 | 提示文案 | 来源 |
|------|---------|---------|---------|------|
| AC-ERR-01 | models.json 存在语法或字段类型错误 | 保留上一个可用模型列表（部分可用态，AC-RULE-20），面板不崩溃，再次重读失败仍维持部分可用；首次启动无旧列表时呈现空列表与解析错误，不空白、不崩溃；修复文件后打开选择器自动恢复 | "配置文件解析失败：models.json 第 X 行（字段 cost 应为数字）" | PRD 11.1 |
| AC-ERR-02 | 自定义模型条目与内置模型 ID 冲突 | 按 pi 合并语义仅呈现一条（自定义覆盖内置），无错误提示 | — | PRD 11.1 |
| AC-ERR-03 | 模型费率元数据缺失 | 成本显示"未知费率"，不显示 0 | "该模型未配置费率" | PRD 11.1 |
| AC-ERR-04 | 会话文件损坏或截断（同步盘冲突） | 会话文件存在可解析消息时部分加载并标注截断点；完全不可解析时跳过该会话；历史列表其余会话不受影响 | "会话文件损坏，已跳过：xxx"（部分加载时提示截断点位置） | PRD 11.1 |
| AC-ERR-05 | 历史会话使用的模型已下架或改名 | 提示选择替代模型；会话其余内容可加载，不拒载整条会话 | "此会话使用的模型不可用，请选择替代模型" | PRD 11.1 |
| AC-ERR-06 | 流式中用户按 Esc（含连续多次按 Esc） | 立即中断，Tab 转入中断态（AC-RULE-03）；已生成内容按 pi 原生格式落盘为不完整轮次并保留展示（PRD 8.3）；可重试（同轮续跑补全）且可继续追问（恢复流程见 AC-OP-16）；Plan 规划中触发时 Tab 转中断态、Plan 保持规划中（AC-RULE-05），见 AC-ERR-26 | —（Esc 中断本身即为反馈） | PRD 11.2 |
| AC-ERR-07 | 网络反复断续 | 同一请求自动重试，上限 3 次后停止并保留已生成内容（按 pi 原生格式落盘为不完整轮次，AC-RULE-30）；Tab 转入中断态（AC-RULE-03），恢复见 AC-OP-16，队列消息按 AC-ERR-19 处置；Plan 执行中触发时按 AC-ERR-14 口径转入失败暂停（AC-RULE-42） | "网络异常，已重试 3 次仍失败，内容已保留" | PRD 11.2 |
| AC-ERR-08 | 工具执行挂起（测试命令不退出） | 用户按 Esc 中断执行 | 1) 已完成部分保留 2) 普通 Tab：转入中断态（AC-RULE-03），回滚经变更清单（AC-FN-46）触发 AC-OP-04 3) Plan 执行中：按 AC-OP-09 处理（已中断 AC-RULE-09、未执行步骤已取消 AC-RULE-25、按步骤回滚 AC-OP-10） 4) 被中断的工具调用状态转失败（AC-RULE-15），结果以失败/中断回传 AI（联动 AC-ERR-20 口径），消息流留痕 | PRD 11.2 |
| AC-ERR-09 | Apply 预览与确认之间目标文件被外部修改 | 阻止写入，要求重新预览 | "文件已被外部修改，请重新预览" | PRD 11.2 |
| AC-ERR-10 | provider 凭证被拒（401/403） | 区分于网络错误展示；流式已生成内容保留；普通 Tab 转入中断态（AC-RULE-03），修复凭证后点击重试即恢复（AC-OP-16），无需重启 VS Code；Plan 执行中触发时转入失败暂停（AC-RULE-42），修复凭证后从当前步续跑 | "凭证被拒绝（401），请在终端更新 Pi 凭证" | PRD 11.3 |
| AC-ERR-11 | `~/.pi/agent` 目录权限不足或被锁定 | 报配置读取失败，不报未安装；解除占用后经 AC-FN-03 重新检测恢复 | "无法读取 Pi 配置（权限不足或被占用）" | PRD 11.3 |
| AC-ERR-12 | AI 修改第 N 个文件时被权限拒绝 | 前 N-1 个文件的修改保留；清单标出失败点，不全部回滚；Plan 步骤内触发时转入失败暂停（AC-RULE-42），授权后经 AC-OP-28 从当前步续跑；普通对话触发时失败结果回传 AI（AC-ERR-20），授权后再次发起，已改文件按 AC-OP-04 可回滚 | "第 X 个文件修改被拒绝，其余已完成" | PRD 11.3 |
| AC-ERR-13 | 压缩请求本身失败（provider 报错） | 保持原上下文不变；执行不无声中断；提示条在仍超阈值时重新出现，可再次点击压缩或执行手动 `/compact`（AC-FN-19）；Plan 执行中触发时执行继续、不转入失败暂停（AC-RULE-42） | "压缩失败，已保留完整上下文" | PRD 11.4 |
| AC-ERR-14 | provider 连续限流 | 自动重试 3 次（与 AC-RULE-30 同一上限）后暂停在当前步（失败暂停态，AC-RULE-42）；Plan 执行不把失败步骤标记为完成 | "请求被限流，已暂停，可稍后继续" | PRD 11.4 |
| AC-ERR-15 | `~/.pi/agent` 位于同步盘、中文或空格路径 | 可读取；读取瞬间的竞态不崩溃、不写坏配置；读取失败时报配置读取失败，恢复经 AC-FN-03 重新检测 | "配置读取失败" | PRD 11.4 |
| AC-ERR-16 | 用户拒绝某工具调用 | 拒绝结果回传 AI；AI 继续对话，会话不中断 | — | PRD 9.3, 8.2 |
| AC-ERR-17 | models.json 中部分 provider 凭证失效 | 失效 provider 对应条目置灰并显示失效标识，悬浮显示原因；其余 provider 条目可用，不整体失败；更新凭证后重新打开选择器即恢复可用 | — | PRD 8.3 |
| AC-ERR-18 | 技能目录中某个技能文件损坏（无法解析） | 打开技能菜单 | 1) 仅该损坏条目跳过并提示 2) 其余技能条目正常呈现与调用 | PRD 9.1, 11.1 |
| AC-ERR-19 | Tab 流式中断（Esc 或网络失败）时 FollowUp 队列中仍有消息 | 查看队列提示并处置 | 1) 队列消息保留，不自动发送、不静默丢弃 2) 恢复后提供"逐条发送"确认或"一键清空"操作，由用户选择 3) 直接关闭处于中断态的 Tab 时弹确认"队列中有 N 条未发送消息，确认丢弃？"，不静默丢弃 | PRD 8.3, 8.5 |
| AC-ERR-20 | 普通对话 Tab（非 Plan）某次工具调用执行失败（非凭证/限流类） | 查看消息流与 Tab 状态 | 1) 失败结果作为工具结果回传 AI，会话不中断，AI 可继续对话 2) 已产生的文件改动保留，可按 AC-OP-04 回滚 3) Plan 执行中步骤/工具失败的场景见 AC-FN-31（转入失败暂停，AC-RULE-42） | PRD 8.2, 11.2 |
| AC-ERR-21 | 普通对话 Tab（非 Plan）流式中遭遇连续限流 | 等待自动重试 | 1) 自动重试 3 次（AC-RULE-30）后中断，Tab 转入中断态（AC-RULE-03） 2) 已生成内容保留，可稍后重试（恢复见 AC-OP-16） | PRD 11.4 |
| AC-ERR-22 | Tab 处于流式中或中断态 | 关闭该 Tab | 1) 流式中关闭隐含中断，已生成内容按 pi 原生格式落盘为不完整轮次（重试后本轮补全，会话文件不留损坏记录） 2) 队列消息随会话保留，重新加载该会话时呈现 AC-ERR-19 处置入口（逐条发送确认/一键清空），不静默丢弃 3) 其他 Tab 不受影响 | PRD 8.5, 11.2 |
| AC-ERR-23 | Inline Chat 正在生成 | 生成过程中断（Esc 或网络错误） | 1) 行内呈现错误提示 2) 文件与选区保持原状，无部分写入 3) 可立即重试 4) 所属 Tab 会话状态转入中断（AC-RULE-03），恢复见 AC-OP-16 5) 行内错误条提供"查看已生成结果"（pending diff 置灰呈现），可查看后选择重发或丢弃 | PRD 9.4, 11.2 |
| AC-ERR-24 | AI 即将通过工具写入文件，且该文件存在未保存手改/外部修改，或另一 Tab 正并行写同一文件（普通对话与 Plan 执行均适用） | 写入前检测命中 | 1) 提示冲突"文件已被修改，是否仍要写入？"，由用户决定，不静默覆盖，双方改动保留 2) 多 Tab 并行写同文件时先行警告 3) Plan 执行中触发：暂停该步骤等待用户决定（失败暂停态，AC-RULE-42）——选择继续覆盖则从当前步骤继续执行该步骤（生成 diff/checkpoint，对应续跑出口）；选择跳过则经调整计划重新批准出口处理（AC-OP-28 重新批准路径，调整计划移除该步骤后重新批准），不静默跳过 | PRD 11.2 |
| AC-ERR-25 | 已打开的会话文件被外部删除或移动 | 在该会话 Tab 中发送消息 | 1) Tab 呈现"会话不存在"错误态 2) 发送被阻止并引导新建会话 3) 不产生写失败丢消息 | PRD 11.3 |
| AC-ERR-26 | Plan 规划中 AI 正在产出计划（Tab 流式中） | Esc 或网络中断后点击重试 | 1) Tab 转入中断态（AC-RULE-03），已生成分析内容按 pi 原生格式落盘为不完整轮次 2) Plan 保持规划中（AC-RULE-05），不误转待批准或失败暂停 3) 重试后继续产出计划并进入待批准（AC-RULE-06，衔接 AC-FN-51） | PRD 6.3, 8.3 |
| AC-ERR-27 | 用户已在压缩提示上选择"暂不"，上下文达到窗口上限且请求被 provider 以超窗拒绝 | 在该 Tab 发送消息或观察当前请求结果 | 1) 呈现明确提示"上下文已满，无法继续发送"，不呈现无引导的 provider 报错 2) 引导手动 /compact（AC-FN-19）或新建会话（AC-FN-57） 3) 已落盘会话内容不受影响 | PRD 11.4 |
| AC-ERR-28 | 会话已按轮落盘（AC-RULE-40），VS Code 进程崩溃或重启 | 重启后重新打开插件并加载该会话 | 1) 落盘轮次完整呈现，Tab 重建后上下文不丢失 2) 未完成轮次按不完整轮次呈现并提供续跑补全入口（AC-FN-05 口径） 3) 队列未发送消息按 AC-ERR-19 处置 | PRD 11.4 |

### 2.5 非功能需求（AC-NF-{NN}）

| 编号 | 验收项 | 指标 | 条件 | 来源 |
|------|-------|------|------|------|
| AC-NF-01 | 插件激活耗时 | ≤ 1.5 秒，首次打开面板无阻塞 | 插件激活 | PRD 13.1 |
| AC-NF-02 | 流式渲染增量更新 | 每个 delta 仅更新受影响 DOM 块；滚动期间无整块重绘闪烁 | ≥2000 token 流式回复；滚动帧率不低于 60fps | PRD 13.1 |
| AC-NF-03 | 会话列表加载 | 500 条会话首屏加载 ≤ 1 秒（虚拟化或分页实现）；搜索过滤随输入呈现结果且无整页刷新 | 500 条历史会话 | PRD 13.1 |
| AC-NF-04 | 模型选择器过滤 | 条目过滤耗时 ≤ 100 毫秒 | 100+ 模型条目 | PRD 13.1 |
| AC-NF-05 | 历史会话加载 | 加载耗时 ≤ 2 秒；加载后呈现该会话最终消息状态 | 单会话 500+ 消息 | PRD 13.1 |
| AC-NF-06 | 连续中断与重发 | 状态机无死锁，后续操作仍可响应 | 连续执行中断、再发、再中断 | PRD 13.1 |
| AC-NF-07 | 首条消息时效 | ≤ 30 秒 | 已配置 Pi 的用户从打开面板到发出首条消息 | PRD 2.2 |
| AC-NF-08 | 凭证安全 | API key 仅存 `~/.pi/agent` 与 SecretStorage；不落明文设置、不写日志、不上报 | 全部涉及凭证的路径 | PRD 13.2 |
| AC-NF-09 | IDE Bridge 安全 | 本地令牌认证沿用，监听范围不扩大 | 全部 RPC 调用 | PRD 13.2 |
| AC-NF-10 | Webview 安全 | 沿用既有 CSP 沙箱策略，限制不降低 | 新增功能全部页面 | PRD 13.2 |
| AC-NF-11 | 平台兼容 | VS Code ≥ 1.100；Windows、macOS、Linux 桌面版 | 发布目标环境 | PRD 13.3 |
| AC-NF-12 | Pi SDK 兼容 | 经唯一网关 `loadPiSdk()` 加载并 feature-detect；SDK 版本差异以当前界面语言的通俗文案提示，不出现未翻译的 SDK/API 术语，包含影响说明与建议动作 | SDK 升级场景 | PRD 13.3, 10 |
| AC-NF-13 | 发布资产 | 图标（marketplace 彩色图标与 36x36 灰阶规范图标）、README、分类标签、版本号齐备；Marketplace 审核通过并上架可安装 | Marketplace 提审 | PRD 4 |
| AC-NF-14 | 既有能力零回归 | 单元测试与集成测试全绿；diff、checkpoint、会话管理行为不变 | 全部既有能力 | PRD 4 |
| AC-NF-15 | 错误提示文案本地化 | 遍历 AC-ERR 全部条目触发对应错误 | 1) 全部错误提示为当前界面语言 2) 每条提示包含影响说明与建议动作 3) 不出现未翻译的 SDK/API 术语堆砌（AC-NF-12 口径） | PRD 13.4 |

### 2.6 角色验收索引

| 角色 | GOAL | 核心验收 AC | 角色特有行为 |
|------|------|-------------|-------------|
| 已配置 Pi 的新装用户 | G-1, G-2, G-7 | AC-FN-01~03, AC-FN-07~09, AC-OP-01, AC-NF-07 | 首次安装即验证配置自动读取；无配置向导（Out of Scope） |
| 日常重度使用者 | G-2, G-4, G-5 | AC-FN-10~19, AC-FN-25~37, AC-FN-56~60, AC-OP-02~17, AC-OP-23, AC-OP-26~32, AC-NF-02, AC-NF-06 | 高频工具调用依赖审批记忆（AC-OP-05~07、AC-OP-26~32）与键盘可达（AC-FN-37） |
| Pi 生态存量用户 | G-3, G-6 | AC-FN-38~41, AC-FN-50, AC-OP-18, AC-OP-20~22, AC-ERR-04~05, AC-ERR-25 | 双端续聊与 token 成本口径一致（AC-RULE-27） |

---

## 3. UI 验收标准（UI-IXD / UI-ELEM）

UI 原型尚未产出（PRD M15 第三批经 `/zcode:ui` 产出界面改版与图标原型）。样式类验收（UI-ELEM）与交互元素细则（UI-IXD）待原型产出后从原型提取补充；本版本中全部交互行为验收已并入 AC-FN 与 AC-OP，不因缺少 UI 编号产生验收缺口。本节为占位说明，不作为 e2e 覆盖分母。

---

## 4. 附录：17 项核心路径覆盖对照

| 范围项（PRD 5.1） | 覆盖的 AC |
|------------------|-----------|
| 1 Pi 配置零配置接入 | AC-FN-01~06, AC-FN-44~45, AC-ERR-01, AC-ERR-11, AC-ERR-15, AC-ERR-17, AC-ERR-18, AC-OP-01, AC-NF-07 |
| 2 图片输入 | AC-FN-07~08 |
| 3 代码块语法高亮 + Apply | AC-FN-09~11, AC-OP-11, AC-ERR-09 |
| 4 流式渲染性能优化 | AC-FN-12, AC-FN-52, AC-ERR-06~07, AC-ERR-19, AC-ERR-21~22, AC-ERR-26, AC-OP-29, AC-NF-02 |
| 5 工具审批按工具名记忆 | AC-FN-13~16, AC-OP-05~07, AC-OP-26~27, AC-OP-31~32, AC-ERR-16, AC-RULE-32~34 |
| 6 上下文阈值询问式自动压缩 | AC-FN-17~19, AC-OP-14, AC-OP-30, AC-ERR-13, AC-ERR-27, AC-RULE-29 |
| 7 选区右键菜单 | AC-FN-20 |
| 8 @-mention | AC-FN-21~23 |
| 9 拖拽文件进对话 | AC-FN-24 |
| 10 真 Inline Chat | AC-FN-25~28, AC-FN-56, AC-OP-12~13, AC-ERR-23 |
| 11 Plan 模式 | AC-FN-29~32, AC-FN-43, AC-FN-48, AC-FN-51, AC-FN-54~55, AC-FN-58~59, AC-OP-08~10, AC-OP-28, AC-ERR-10, AC-ERR-14, AC-ERR-24, AC-ERR-26, AC-RULE-38, AC-RULE-42 |
| 12 TODO 任务进度卡片 | AC-FN-31, AC-FN-33, AC-OP-08 |
| 13 终端集成 | AC-FN-34 |
| 14 commit message 生成 | AC-OP-15 |
| 15 子代理/后台任务面板 | AC-FN-35, AC-OP-17 |
| 16 界面与图标整体重设计 | AC-FN-42, AC-NF-13 |
| 17 界面多语言 | AC-FN-36, AC-OP-20~21 |
| 横切：会话互操作与单写者 | AC-FN-38~41, AC-FN-50, AC-FN-57, AC-OP-18~19, AC-OP-22, AC-ERR-04~05, AC-ERR-25, AC-ERR-28 |
| 横切：diff/checkpoint 回滚 | AC-FN-46, AC-OP-03~04, AC-OP-10, AC-ERR-08, AC-ERR-12, AC-ERR-20 |
| 横切：E2E 全循环（PRD 14） | AC-FN-60 |
